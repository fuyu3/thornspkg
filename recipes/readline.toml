name        = "readline"
version     = "8.2"
description = "Edição de linha de comando e histórico interativo"
depends     = ["ncurses"]
source      = "https://ftp.gnu.org/gnu/readline/readline-8.2.tar.gz"
# sha256 = "preencha com: sha256sum readline-8.2.tar.gz"
build_system = "autotools"
configure_args = ["--with-curses", "--disable-static"]
post_install   = ["ldconfig"]
